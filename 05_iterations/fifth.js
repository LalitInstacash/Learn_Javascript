const coading = ["js", "cpp", "ruby", "java", "python"]

coading.forEach( function (params) {
    // console.log(params);
} )

const num = [1,2,3,4,5]
num.forEach( function (number) {
    // console.log(number);
} )

